{
  "name": "dodo-games",
  "version": "1.0.0",
  "description": "Collection of Dodo games for Cloudflare Pages",
  "scripts": {
    "build": "node build.js",
    "preview": "npx wrangler pages dev dist",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=dodo-games"
  },
  "keywords": [
    "games",
    "cloudflare-pages"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "clean-css": "^5.3.3",
    "fs-extra": "^11.3.2",
    "glob": "^13.0.0",
    "terser": "^5.44.1"
  }
}
